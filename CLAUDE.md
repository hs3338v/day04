# 홍길동 포트폴리오

디자인 작업물을 소개하는 개인 포트폴리오 사이트.

## 핵심 기능

- 자기소개
- 작업물 목록
- 작업물 상세
- 로그인
- 작업물 등록/수정/삭제

## 현재 상태

정적 HTML/CSS 페이지 몇 개로 시작한 단계이며, 위 핵심 기능 대부분은 아직 구현되지 않았다.

- `index.html` — 랜딩/네비게이션 허브
- `about.html` — 자기소개 페이지 자리
- `preview.html` — Supabase(`test_todos` 테이블)에서 데이터를 fetch로 불러와 목록으로 보여주는 연습 페이지. 작업물 목록/상세 기능을 만들 때 이 fetch 패턴을 참고할 것
- `styles.css` — 전 페이지 공통 스타일시트

로그인, 작업물 CRUD(등록/수정/삭제)는 아직 없음 — 구현 시 Supabase Auth + 테이블을 사용할 가능성이 높음(이미 preview.html에서 Supabase REST API를 쓰고 있음).

## 디자인 원칙

기존에 "AI가 만든 티가 나는" 디자인(그라디언트 배경, 글로우 카드, 알약형 네비게이션 버튼, 범용 시스템 폰트)을 지양하라는 피드백을 받았다. 대신:

- Fraunces(세리프, 헤드라인용) + Inter(산세리프, 본문용) 폰트 조합
- 카드/그림자 대신 헤어라인(1px 구분선) 위주의 절제된 레이아웃
- 강조색은 한 가지(현재는 muted gold `#a8874f`)만 아껴서 사용
- 네비게이션은 버튼이 아니라 밑줄 텍스트 링크
- 둥근 모서리를 남발하지 않음

새 페이지나 컴포넌트를 만들 때는 `styles.css`의 기존 토큰(`--ink`, `--bg`, `--line`, `--accent`)과 클래스 네이밍 스타일을 따라갈 것.

## 커밋

커밋 메시지는 한국어로 작성한다.

## 폰트 규칙

Fraunces는 한글 글리프가 없는 라틴 전용 세리프라, 한글 텍스트는 원래도 시스템 폰트로 폴백되고 있었다. 이를 명시적으로 고정한다.

- 워드마크(`day04`)와 숫자 등 라틴 문자는 Fraunces를 유지한다.
- 한글이 들어가는 헤드라인/본문은 `font-family` 폴백 순서에 `"Malgun Gothic"`을 라틴 폰트 다음으로 반드시 포함시켜, 브라우저의 임의 폴백에 맡기지 않고 맑은 고딕으로 렌더링되게 한다.
- 맑은 고딕은 Windows 전용 폰트이므로, 다른 OS를 위해 마지막 폴백으로 `serif`/`sans-serif`를 남겨둔다.